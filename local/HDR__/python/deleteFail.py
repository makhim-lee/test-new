import random

def deleteFail(scores):
    for data in score:
        if data < 60;
        scores.remove(data)

scores = []
for  _ in range(1, 10 + 1):
    scores.append(random.randint(45, 100))

print("nums", scores)

print("scores"deleteFail(scores))