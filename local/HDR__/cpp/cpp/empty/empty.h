#ifndef EMPTY_H
#define EMPTY_H

class Empty{

public:
    //Empty();   기본 생성자 다른 생성자가 없을때 컴파일러가 만들어줌 
    //~Empty();

    //Empty(const Empty&); 기본 복사 생성자
    //Empty& operator=(const Empty&); 기본치환생성자

    //Empty *operator&();   //겍채의 시작 주소 구함

    //const Empty *operator&() const; 
};

#endif