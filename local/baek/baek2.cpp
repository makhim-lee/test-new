// b진수 숫자 n --> 10진법
// N, B
// b^0 * n / b^1 * n / .... b^i * n

#include <iostream>
#include <cstring>

int main()
{   
    char arr[100];
    int notation;
    int root;
    int ten;
    std::cin >> arr >> notation;


    for(int i = 0; i < strlen(arr); i++) {
        for(int i = 0; i < strlen(arr); i++){
            
        }
        ten = arr[i] * strlen(arr) - i
    }

    return 0;
}
